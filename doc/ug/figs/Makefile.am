graphics = glide.eps glimmer.eps
EXTRA_DIST = glide.dia glimmer.dia $(graphics)
if MAINTAINER_MODE
 BUILT_SOURCES = $(graphics)
 CLEANFILES = $(BUILT_SOURCES)
endif
include $(top_srcdir)/doc/make_doc.rules
